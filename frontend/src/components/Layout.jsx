const Layout = ({ children }) => {
  return (
    <>
      {/* <Header>Header Content</Header> */}
      <main>{children}</main>
      {/* <Footer>Footer Content</Footer> */}
    </>
  );
};

export default Layout;
